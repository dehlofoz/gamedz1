
const endpoints = require("./config")
const { getData } = require("./api-utils")
const { getRandomGame } = require("./api-utils")

module.exports = {
    endpoints,
    getData,
    getRandomGame, 
    
}